{
  "name": "cwc---content-with-coffee",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-install": "ts-node check-install.ts",
    "server": "cd backend-cwc && npx ts-node app.ts",
    "client": "cd frontend-cwc && npm run dev",
    "dev": "npm run check-install && concurrently --raw \"npm run server\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.4",
    "ts-node": "^10.9.2"
  }
}
